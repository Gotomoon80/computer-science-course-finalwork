<div style="font-size: 18px;">

# 环境准备完全指南

## 📌 开始制作前的准备工作

### ✅ 准备清单

| 序号 | 准备项 | 状态 | 说明 |
|------|-------|------|------|
| 1 | 安装Python 3.9+ | ⬜ | 必需，开发语言 |
| 2 | 安装VS Code | ⬜ | 推荐，代码编辑器 |
| 3 | 安装项目依赖 | ⬜ | 必需，3个库 |
| 4 | 下载UI素材 | ⬜ | 可选，美化用 |
| 5 | 创建项目结构 | ⬜ | 必需，目录框架 |

---

## 🐍 Step 1: 安装Python（15分钟）

### 1.1 下载Python

**官方下载地址**：https://www.python.org/downloads/

**选择版本**：Python 3.9.13 或更高版本（推荐 3.9.13）

### 1.2 安装步骤（重要！）

1. **运行安装程序**
2. **✅ 勾选 "Add Python to PATH"** ⬅️ 非常重要！
3. 点击 "Install Now"
4. 等待安装完成
5. 点击 "Close"

### 1.3 验证安装

打开PowerShell（或命令提示符），输入：

```powershell
python --version
```

应该看到：`Python 3.9.13` 或类似版本号

```powershell
pip --version
```

应该看到：`pip 22.x.x from ...`

如果都能正常显示，说明安装成功！✅

---

## 💻 Step 2: 安装VS Code（10分钟，可选但推荐）

### 2.1 下载VS Code

**官方下载**：https://code.visualstudio.com/

选择 Windows 版本下载

### 2.2 安装插件

安装完成后，打开VS Code，安装以下插件：

1. **Python** (Microsoft) - 必需
2. **Pylance** - 推荐
3. **Chinese Language Pack** - 中文界面

在VS Code左侧点击扩展图标（四个方块），搜索安装即可。

---

## 📦 Step 3: 安装项目依赖（5分钟）

### 3.1 创建requirements.txt

在项目文件夹创建文件 `requirements.txt`，内容如下：

```
pyinstaller==5.13.0
Pillow==10.0.0
ttkthemes==3.2.2
ntplib==0.4.0
```

### 3.2 一键安装所有依赖

打开PowerShell，进入项目目录：

```powershell
cd "d:\computer science course finalwork"

# 安装所有依赖
pip install -r requirements.txt
```

等待安装完成（约2-3分钟）

### 3.3 验证安装

```powershell
# 验证各个库
python -c "import tkinter; print('Tkinter OK')"
python -c "import sqlite3; print('SQLite OK')"
python -c "from PIL import Image; print('Pillow OK')"
python -c "import ntplib; print('ntplib OK')"
python -c "import PyInstaller; print('PyInstaller OK')"
```

如果都显示 OK，说明安装成功！✅

---

## 🎨 Step 4: 准备UI素材（30分钟，可选）

### 4.1 下载图标素材

**推荐网站**：
- Flaticon: https://www.flaticon.com/
- Iconfinder: https://www.iconfinder.com/
- Icons8: https://icons8.com/

**需要的图标**（建议64x64像素，PNG格式）：

| 图标 | 搜索关键词 | 数量 |
|------|-----------|------|
| 定时器图标 | timer icon | 1个 |
| 计算器图标 | calculator icon | 1个 |
| 待办图标 | todo icon | 1个 |
| 番茄钟图标 | tomato icon | 1个 |
| 开始按钮 | play icon | 1个 |
| 暂停按钮 | pause icon | 1个 |
| 重置按钮 | reset icon | 1个 |
| 设置图标 | settings icon | 1个 |

**下载后放在**：`resources/icons/` 文件夹

### 4.2 制作应用图标

**在线工具**：https://convertio.co/zh/png-ico/

上传一个PNG图片，转换成ICO格式，保存为 `app_logo.ico`

### 4.3 选择配色方案

从文档中选择一套配色方案，记录RGB值：

**清新蓝方案**（推荐）：
```python
PRIMARY_COLOR = "#4A90E2"      # 主色调
SECONDARY_COLOR = "#50C9CE"    # 辅助色
ACCENT_COLOR = "#F5A623"       # 强调色
BACKGROUND_COLOR = "#F8F9FA"   # 背景色
TEXT_COLOR = "#333333"         # 文字色
```

---

## 📁 Step 5: 创建项目结构（5分钟）

### 5.1 自动创建脚本

我已经为你准备了一个脚本，运行即可创建完整目录结构。

保存以下内容为 `create_structure.py`：

```python
import os

# 项目根目录
project_root = "d:/computer science course finalwork"

# 目录结构
dirs = [
    "modules",
    "gui",
    "utils",
    "data",
    "data/logs",
    "resources",
    "resources/icons",
    "resources/images"
]

# 创建目录
for d in dirs:
    path = os.path.join(project_root, d)
    os.makedirs(path, exist_ok=True)
    print(f"✓ 创建目录: {d}")
    
    # 为Python包创建__init__.py
    if d in ["modules", "gui", "utils"]:
        init_file = os.path.join(path, "__init__.py")
        open(init_file, 'w').close()
        print(f"  + 创建 __init__.py")

# 创建主要文件
files = [
    "main.py",
    "config.json",
    "requirements.txt",
    "modules/timer.py",
    "modules/calculator.py",
    "modules/todo.py",
    "modules/statistics.py",
    "gui/main_window.py",
    "gui/timer_window.py",
    "gui/calc_window.py",
    "gui/todo_window.py",
    "utils/database.py",
    "utils/time_sync.py",
    "utils/clock.py",
    "utils/validator.py"
]

for f in files:
    path = os.path.join(project_root, f)
    if not os.path.exists(path):
        open(path, 'w', encoding='utf-8').close()
        print(f"✓ 创建文件: {f}")

print("\n🎉 项目结构创建完成！")
print(f"📁 项目路径: {project_root}")
```

### 5.2 运行脚本

```powershell
python create_structure.py
```

---

## 🚀 完成准备后的项目结构

```
d:\computer science course finalwork\
├── 📄 README.md                            (已有)
├── 📄 期末作业设计思路.md                  (已有)
├── 📄 项目实施步骤手册.md                  (已有)
├── 📄 离线运行与网络授时实现指南.md        (已有)
├── 📄 00-环境准备指南.md                   (本文档)
├── 📄 create_structure.py                  (待创建)
├── 📄 main.py                              (待编写)
├── 📄 config.json                          (待编写)
├── 📄 requirements.txt                     (待创建)
│
├── 📁 modules/                             (功能模块)
│   ├── __init__.py
│   ├── timer.py                           (定时器)
│   ├── calculator.py                      (计算器)
│   ├── todo.py                            (待办事项)
│   └── statistics.py                      (数据统计)
│
├── 📁 gui/                                 (界面模块)
│   ├── __init__.py
│   ├── main_window.py                     (主窗口)
│   ├── timer_window.py                    (定时器界面)
│   ├── calc_window.py                     (计算器界面)
│   └── todo_window.py                     (待办界面)
│
├── 📁 utils/                               (工具类)
│   ├── __init__.py
│   ├── database.py                        (数据库管理)
│   ├── time_sync.py                       (网络授时)
│   ├── clock.py                           (高精度时钟)
│   └── validator.py                       (输入验证)
│
├── 📁 data/                                (数据目录)
│   ├── study.db                           (数据库，运行后生成)
│   └── logs/                              (日志)
│
└── 📁 resources/                           (资源文件)
    ├── icons/                             (图标)
    │   ├── timer_icon.png
    │   ├── calculator_icon.png
    │   └── ...
    ├── images/                            (图片)
    └── app_logo.ico                       (应用图标)
```

---

## ✅ 准备工作完成检查清单

完成以下所有项后，就可以开始编码了：

### 环境检查
- [ ] Python 已安装并添加到PATH
- [ ] `python --version` 显示版本号
- [ ] `pip --version` 显示版本号
- [ ] VS Code 已安装（可选）

### 依赖检查
- [ ] requirements.txt 已创建
- [ ] 所有依赖已安装（pip install -r requirements.txt）
- [ ] 导入测试全部通过

### 项目结构检查
- [ ] 所有目录已创建
- [ ] 所有__init__.py已创建
- [ ] 空白.py文件已创建

### 素材准备（可选）
- [ ] 图标素材已下载
- [ ] app_logo.ico 已准备
- [ ] 配色方案已选择

---

## 🎯 下一步：开始编码

准备工作完成后，按以下顺序开始编码：

### Day 1: 核心工具类
1. `utils/clock.py` - 高精度时钟
2. `utils/time_sync.py` - 网络授时
3. `utils/database.py` - 数据库管理

### Day 2-3: 定时器模块
4. `modules/timer.py` - 定时器核心逻辑
5. `gui/timer_window.py` - 定时器界面

### Day 4: 其他功能
6. `modules/todo.py` - 待办事项
7. `modules/calculator.py` - 计算器
8. `gui/main_window.py` - 主窗口

### Day 5: 集成测试
9. `main.py` - 主程序入口
10. 测试所有功能

### Day 6: 打包发布
11. 使用PyInstaller打包
12. 测试exe文件

---

## 💡 快速开始建议

如果你想快速开始，可以：

1. **先安装Python环境**（15分钟）
2. **安装依赖库**（5分钟）
3. **运行create_structure.py创建结构**（1分钟）
4. **开始编写第一个文件：utils/clock.py**

这样你就可以立即开始编码了！

---

## 🆘 遇到问题？

### Python安装失败
- 确保从官网下载
- 以管理员身份运行安装程序
- 必须勾选"Add to PATH"

### pip安装依赖失败
- 检查网络连接
- 尝试使用国内镜像：
  ```powershell
  pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
  ```

### VS Code无法识别Python
- 重启VS Code
- 手动选择Python解释器：Ctrl+Shift+P → "Python: Select Interpreter"

---

**准备好了吗？Let's go! 🚀**

</div>
